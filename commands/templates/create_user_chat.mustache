'use strict'

const Schema = use('Schema')

class TwilioUserChat extends Schema {
    up () {
        this.create('twilio_user_chats', (table) => {
            table.increments()
            table.integer('user_id').notNullable()
            table.string('chat_id').notNullable()
            table.timestamps()
        })
    }

    down () {
        this.drop('twilio_users')
    }
}

module.exports = TwilioUserSchema
